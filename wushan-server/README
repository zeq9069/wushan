巫山（第一期设计）

背景：
 	在策略上线的时候，我们需要对策略进行验证正确性（包括语法和误拦截情况），语法错误很容易就验证了通过单元测试，但是对于该策略是否导致误拦，
 	这种情况根本不可能去预估计，所以我们需要有这样的一个系统，将我们配置的策略环境打包上传，自动帮我们过一边过去N天的数据，并把这些数据落地，提供
 	策略产品去分析，这样的一个“计算+存储”的系统，一定是分布式的，分布式的计算和存储。
 	
功能设计:
	分布式计算+分布式存储：
		我们采用master—slave的结构模式，master提供数据和计算的路由分发，data节点负责存储数据和数据的计算。
		
协议设计：
 
 消息包必备前缀：#EFBD(2byte) + 项目版本号（2byte） + 消息类型（1byte） + 协议类型（1byte）
 
 通信接口：
 	1.数据传输
 	2.心跳信息（传输机器信息）
 	3.传输task
 	4.获取任务当前运行状态
 	5.response
 